let cats = ['bob','willy','mini'];
cats.pop() //Removes the last item 
cats.push('George') //Adds an item to the end of array
cats.shift() //Remove an item from beginning
cats.unshift('Puff'); //Adds and item in the beginning
console.log(cats);
